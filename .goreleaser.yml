version: 2
project_name: termyard
release:
  github:
    owner: anh-chu
    name: termyard
  draft: false
  # v* tags are stable releases; nightly builds are snapshots and do not create releases here.
  prerelease: auto
  name_template: "{{ .Tag }}"
before:
  hooks:
    - make frontend
builds:
  - id: termyard
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s
      - -w
      - -extldflags="-static"
      - -X '{{ .ModulePath }}/pkg/common.SUMMARY=v{{ .Version }}'
      - -X '{{ .ModulePath }}/pkg/common.BRANCH={{ .Branch }}'
      - -X '{{ .ModulePath }}/pkg/common.VERSION={{ .Tag }}'
      - -X '{{ .ModulePath }}/pkg/common.COMMIT={{ .Commit }}'
    mod_timestamp: "{{ .CommitTimestamp }}"
archives:
  - id: termyard
    ids:
      - termyard
    name_template: "{{ .ProjectName }}-v{{ .Version }}-{{ .Os }}-{{ .Arch }}{{ .Arm }}"
checksum:
  name_template: "checksums.txt"
snapshot:
  version_template: '{{ trimprefix .Summary "v" }}'
# Changelog is maintained separately.
changelog:
  disable: true
